[project]
name = "web"
version = "0.1.0"
description = "Minimal Flask web API"
readme = "README.md"
authors = [{ name = "Hassan Jawhar" }]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]
